query ReadProduction($id: ID!) {
  ReadProduction: findOneProduction(id: $id) {
    name
    description
    startTime
    category {
      name
    }
    images {
      image {
        id
        description
        path
      }
    }
    videos {
      video {
        name
        format
        metadata
      }
    }
    tags {
      tag
    }
  }
}
