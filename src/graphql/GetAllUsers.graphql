query GetAllUsers($pagination: Pagination) {
  userCount: countUser
  GetAllUsers: findManyUser(pagination: $pagination) {
    id
    username
    mail
    discord
    joined
    person {
      id
      name
    }
    permissions {
      id
      action
      subject
      fields
      conditions
      inverted
      reason
    }
  }

}
